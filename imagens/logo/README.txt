Coloque aqui um logotipo em arquivo próprio (SVG ou PNG), caso queira usar
um logo separado da foto da mascote no cabeçalho do site.

Por padrão, o cabeçalho (index.html) usa um logo em texto ("PRIGEOSC") com
um selo azul com a letra "P" — não depende de nenhum arquivo desta pasta.
